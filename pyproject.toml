[project]
name = "tennis"
version = "0.1.0"
description = ""
authors = [
    {name = "artik",email = "artikano27@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "sqlalchemy (>=2.0.51,<3.0.0)",
    "pymysql (>=1.2.0,<2.0.0)",
    "jinja2 (>=3.1.6,<4.0.0)",
    "waitress (>=3.0.2,<4.0.0)",
    "dotenv (>=0.9.9,<0.10.0)",
    "alembic (>=1.18.5,<2.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "pytest (>=9.1.1,<10.0.0)"
]

[tool.poetry]
packages = [{include = "*", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
